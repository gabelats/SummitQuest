<li class="list-group-item d-flex justify-content-between align-items-center">
  <div class="ms-2 me-auto">
    <a href="hike/{{this.hikes.id}}">
      <div class="fw-bold">{{this.peak_name}}</div>
    </a>
  </div>
  <span class="badge bg-primary rounded-pill">{{this.elevation}}</span>
</li>